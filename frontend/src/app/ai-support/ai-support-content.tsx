// @generated by scripts/generate-pages.mjs — do not edit by hand
"use client";

import Script from "next/script";
import "./ai-support.css";

export default function AiSupportContent() {
  return (
    <>
      <div dangerouslySetInnerHTML={{ __html: "\n\n<nav class=\"topnav\">\n  <a class=\"nav-logo\" href=\"/\">\n    <div class=\"nav-logo-pill\">HR Playhouse</div>\n    <div class=\"nav-logo-text\">Hub</div>\n  </a>\n  <div class=\"nav-title\">AI HR Support</div>\n  <div class=\"nav-links\">\n    <a class=\"tnl\" href=\"/dashboard\">Dashboard</a>\n    <a class=\"tnl\" href=\"https://learn.thehrplayhousehub.org/courses/\">Courses</a>\n    <a class=\"tnl\" href=\"/playbook\">Playbook</a>\n  </div>\n  <div class=\"nav-right\">\n    <div class=\"disclaimer-badge\">⚠ Guidance only — not legal advice</div>\n  </div>\n</nav>\n\n<div class=\"app-layout\">\n\n  <!-- SIDEBAR -->\n  <aside class=\"sidebar\">\n    <div class=\"sidebar-header\">\n      <div class=\"sh-title\">AI HR Support</div>\n      <div class=\"sh-sub\">Ask any HR question or upload a document for expert analysis.</div>\n    </div>\n\n    <div class=\"suggested-section\">\n      <div class=\"sq-label\">Suggested questions</div>\n      <div class=\"sq-chips\" id=\"sq-chips\">\n        <button class=\"sq-chip\" onclick=\"askSuggested(this)\">How do I handle a disciplinary process fairly under UK law?</button>\n        <button class=\"sq-chip\" onclick=\"askSuggested(this)\">What are the key steps in a redundancy process for 20+ employees?</button>\n        <button class=\"sq-chip\" onclick=\"askSuggested(this)\">How should I respond when an employee discloses a mental health condition?</button>\n        <button class=\"sq-chip\" onclick=\"askSuggested(this)\">What does a CIPD-aligned performance management framework look like?</button>\n        <button class=\"sq-chip\" onclick=\"askSuggested(this)\">What are the legal requirements for flexible working requests in the UK?</button>\n        <button class=\"sq-chip\" onclick=\"askSuggested(this)\">How do I calculate statutory redundancy pay?</button>\n      </div>\n    </div>\n\n    <div class=\"upload-section\">\n      <div class=\"ul-label\">Upload a document</div>\n      <div class=\"upload-zone\" id=\"upload-zone\">\n        <input type=\"file\" id=\"doc-upload\" accept=\".pdf,.doc,.docx,.txt\" onchange=\"handleFileUpload(this)\">\n        <div id=\"upload-default\">\n          <div class=\"uz-icon\">📎</div>\n          <div class=\"uz-text\"><strong>Click to upload</strong> or drag & drop</div>\n          <div class=\"uz-types\">PDF, Word, or TXT · Max 10MB</div>\n        </div>\n        <div class=\"file-preview\" id=\"file-preview\">\n          <div class=\"fp-icon\" id=\"fp-icon\">📄</div>\n          <div class=\"fp-name\" id=\"fp-name\">filename.pdf</div>\n          <button class=\"fp-remove\" onclick=\"removeFile(event)\" title=\"Remove file\">✕</button>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"info-section\">\n      <div class=\"info-card\">\n        <div class=\"ic-title\">What I can help with</div>\n        <div class=\"ic-item\"><div class=\"ic-dot\"></div>Employment law questions (UK, Nigeria, USA, Singapore)</div>\n        <div class=\"ic-item\"><div class=\"ic-dot\"></div>HR policy and process guidance</div>\n        <div class=\"ic-item\"><div class=\"ic-dot\"></div>Analysing HR documents you upload</div>\n        <div class=\"ic-item\"><div class=\"ic-dot\"></div>CIPD and SHRM framework questions</div>\n        <div class=\"ic-item\"><div class=\"ic-dot\"></div>Performance, recruitment, and ER advice</div>\n        <div class=\"ic-item\"><div class=\"ic-dot\"></div>Drafting template language for HR conversations</div>\n      </div>\n    </div>\n  </aside>\n\n  <!-- CHAT -->\n  <div class=\"chat-area\">\n    <div class=\"messages\" id=\"messages\">\n      <!-- WELCOME STATE -->\n      <div class=\"welcome-state\" id=\"welcome-state-initial\">\n        <div class=\"ws-icon\">🤖</div>\n        <div class=\"ws-title\">Your AI HR Advisor</div>\n        <div class=\"ws-sub\">Ask any HR question and get research-backed, evidence-led answers grounded in CIPD frameworks, employment law, and best practice.</div>\n        <div class=\"ws-capabilities\">\n          <span class=\"ws-cap\">🇬🇧 UK law</span>\n          <span class=\"ws-cap\">🇳🇬 Nigerian law</span>\n          <span class=\"ws-cap\">🇺🇸 US law</span>\n          <span class=\"ws-cap\">🇸🇬 Singapore law</span>\n          <span class=\"ws-cap\">📚 CIPD-aligned</span>\n          <span class=\"ws-cap\">📎 Document analysis</span>\n        </div>\n      </div>\n    </div>\n\n    <div class=\"input-area\">\n      <div class=\"disclaimer\">Guidance only — not a substitute for qualified legal advice. Always consult an employment lawyer for specific legal situations.</div>\n      <div class=\"input-row\">\n        <button class=\"new-chat-btn\" onclick=\"newChat()\">+ New chat</button>\n        <div class=\"input-wrap\">\n          <textarea class=\"chat-input\" id=\"chat-input\" placeholder=\"Ask an HR question or describe your situation…\" rows=\"1\" onkeydown=\"handleKeydown(event)\" oninput=\"autoResize(this)\"></textarea>\n        </div>\n        <button class=\"send-btn\" id=\"send-btn\" onclick=\"sendMessage()\" title=\"Send\">↑</button>\n      </div>\n    </div>\n  </div>\n</div>\n\n<script>\n/* ══════════════════════════════════════════════════════════════\n   HR PLAYHOUSE HUB — AI HR SUPPORT\n   Uses Claude API (claude-sonnet-4-20250514) via the Anthropic API\n\n   DEVELOPER NOTE: The Anthropic API does not support direct\n   browser calls (no CORS). This file is designed for:\n   \n   Option A (Recommended): WordPress REST endpoint\n   - Create a WP REST route: POST /wp-json/hrph/v1/chat\n   - That route calls the Anthropic API server-side\n   - Replace API_ENDPOINT below with your WordPress endpoint\n   \n   Option B: Netlify / Vercel serverless function\n   - Deploy a small proxy function\n   - Set ANTHROPIC_API_KEY as an env variable\n   \n   The chat logic, UI, and message handling are complete.\n   Only the API_ENDPOINT URL needs updating.\n══════════════════════════════════════════════════════════════ */\n\nconst API_ENDPOINT = '/wp-json/hrph/v1/chat'; // ← Change this to your proxy endpoint\n\nconst SYSTEM_PROMPT = `You are an expert HR advisor for HR Playhouse Hub, a professional HR learning platform. You provide evidence-based, practical HR guidance to HR professionals at all career stages — from early-career advisors to CHROs.\n\nYour knowledge base includes:\n- CIPD professional frameworks and standards\n- SHRM competency model\n- Gallup engagement research\n- UK employment law (Equality Act 2010, Employment Rights Act 1996, ACAS Codes of Practice)\n- Nigerian Labour Act and National Industrial Court principles\n- US employment law (Title VII, ADA, FMLA, ADEA, at-will doctrine)\n- Singapore Employment Act, MOM guidelines, TAFEP\n- Hong Kong Employment Ordinance\n- Academic HR research and best practice\n\nYour responses are:\n- Practical and actionable — give real guidance, not just principles\n- Jurisdiction-aware — ask which country if it matters for the answer\n- Evidence-based — reference frameworks, legislation, and research when relevant\n- Balanced — acknowledge complexity and nuance in HR situations\n- Professional but accessible — expert tone, not academic jargon\n\nAlways end responses involving employment law with a brief reminder that this is guidance only and a qualified employment lawyer should be consulted for specific legal situations.\n\nIf a document has been shared, analyse it thoroughly and provide specific, detailed feedback relevant to the document type.`;\n\nlet conversationHistory = [];\nlet uploadedFileContent = null;\nlet uploadedFileName = null;\nlet isLoading = false;\n\n/* FILE HANDLING */\nasync function handleFileUpload(input) {\n  const file = input.files[0];\n  if (!file) return;\n  if (file.size > 10 * 1024 * 1024) {\n    alert('File too large. Please upload files under 10MB.');\n    return;\n  }\n  uploadedFileName = file.name;\n  const ext = file.name.split('.').pop().toLowerCase();\n  const iconMap = { pdf: '📕', doc: '📘', docx: '📘', txt: '📄' };\n  document.getElementById('fp-icon').textContent = iconMap[ext] || '📄';\n  document.getElementById('fp-name').textContent = file.name;\n  document.getElementById('upload-default').style.display = 'none';\n  document.getElementById('file-preview').classList.add('show');\n  document.getElementById('upload-zone').classList.add('has-file');\n  // Read as text for TXT files, base64 for others\n  if (ext === 'txt') {\n    uploadedFileContent = await file.text();\n  } else {\n    uploadedFileContent = await toBase64(file);\n  }\n}\n\nfunction toBase64(file) {\n  return new Promise((res, rej) => {\n    const r = new FileReader();\n    r.onload = () => res(r.result.split(',')[1]);\n    r.onerror = rej;\n    r.readAsDataURL(file);\n  });\n}\n\nfunction removeFile(e) {\n  e.stopPropagation();\n  uploadedFileContent = null;\n  uploadedFileName = null;\n  document.getElementById('doc-upload').value = '';\n  document.getElementById('upload-default').style.display = 'block';\n  document.getElementById('file-preview').classList.remove('show');\n  document.getElementById('upload-zone').classList.remove('has-file');\n}\n\n/* CHAT LOGIC */\nfunction autoResize(el) {\n  el.style.height = 'auto';\n  el.style.height = Math.min(el.scrollHeight, 120) + 'px';\n}\n\nfunction handleKeydown(e) {\n  if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendMessage(); }\n}\n\nfunction askSuggested(btn) {\n  document.getElementById('chat-input').value = btn.textContent;\n  sendMessage();\n}\n\nfunction newChat() {\n  conversationHistory = [];\n  uploadedFileContent = null;\n  uploadedFileName = null;\n  document.getElementById('doc-upload').value = '';\n  document.getElementById('upload-default').style.display = 'block';\n  document.getElementById('file-preview').classList.remove('show');\n  document.getElementById('upload-zone').classList.remove('has-file');\n  const msgs = document.getElementById('messages');\n  msgs.innerHTML = `<div class=\"welcome-state\" id=\"welcome-state\">\n    <div class=\"ws-icon\">🤖</div>\n    <div class=\"ws-title\">Your AI HR Advisor</div>\n    <div class=\"ws-sub\">Ask any HR question and get research-backed, evidence-led answers grounded in CIPD frameworks, employment law, and best practice.</div>\n    <div class=\"ws-capabilities\">\n      <span class=\"ws-cap\">🇬🇧 UK law</span>\n      <span class=\"ws-cap\">🇳🇬 Nigerian law</span>\n      <span class=\"ws-cap\">🇺🇸 US law</span>\n      <span class=\"ws-cap\">🇸🇬 Singapore law</span>\n      <span class=\"ws-cap\">📚 CIPD-aligned</span>\n      <span class=\"ws-cap\">📎 Document analysis</span>\n    </div>\n  </div>`;\n}\n\nasync function sendMessage() {\n  if (isLoading) return;\n  const input = document.getElementById('chat-input');\n  const text = input.value.trim();\n  if (!text) return;\n\n  // Hide welcome state\n  const ws = document.getElementById('welcome-state-initial');\n  if (ws) ws.remove();\n\n  // Add user message to UI\n  appendMessage('user', text, uploadedFileName);\n  input.value = '';\n  input.style.height = 'auto';\n\n  // Build message for API\n  let userContent;\n  const ext = uploadedFileName ? uploadedFileName.split('.').pop().toLowerCase() : null;\n\n  if (uploadedFileContent && ext === 'txt') {\n    userContent = `${text}\\n\\n[Document attached: ${uploadedFileName}]\\n\\n${uploadedFileContent}`;\n  } else if (uploadedFileContent && (ext === 'pdf' || ext === 'doc' || ext === 'docx')) {\n    userContent = [\n      { type: 'text', text: text },\n      { type: 'document', source: { type: 'base64', media_type: ext === 'pdf' ? 'application/pdf' : 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', data: uploadedFileContent }, title: uploadedFileName }\n    ];\n  } else {\n    userContent = text;\n  }\n\n  // Clear file after sending\n  const sentFileName = uploadedFileName;\n  if (uploadedFileContent) {\n    uploadedFileContent = null;\n    uploadedFileName = null;\n    document.getElementById('doc-upload').value = '';\n    document.getElementById('upload-default').style.display = 'block';\n    document.getElementById('file-preview').classList.remove('show');\n    document.getElementById('upload-zone').classList.remove('has-file');\n  }\n\n  conversationHistory.push({ role: 'user', content: userContent });\n\n  // Show typing indicator\n  const typingId = showTyping();\n  isLoading = true;\n  document.getElementById('send-btn').disabled = true;\n\n  try {\n    const response = await fetch(API_ENDPOINT, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({\n        model: 'claude-sonnet-4-20250514',\n        max_tokens: 1024,\n        system: SYSTEM_PROMPT,\n        messages: conversationHistory\n      })\n    });\n\n    removeTyping(typingId);\n\n    if (!response.ok) throw new Error(`API error: ${response.status}`);\n    const data = await response.json();\n    const assistantText = data.content?.[0]?.text || 'Sorry, I could not generate a response. Please try again.';\n\n    conversationHistory.push({ role: 'assistant', content: assistantText });\n    appendMessage('assistant', assistantText);\n\n  } catch (err) {\n    removeTyping(typingId);\n    appendMessage('assistant', `I'm sorry — there was an error connecting to the AI service. Please try again in a moment.\\n\\nIf the problem persists, please contact contact@thehrplayhousehub.org for support.`);\n    console.error('AI Support error:', err);\n  }\n\n  isLoading = false;\n  document.getElementById('send-btn').disabled = false;\n}\n\nfunction appendMessage(role, text, docName) {\n  const msgs = document.getElementById('messages');\n  const div = document.createElement('div');\n  div.className = `message ${role}`;\n\n  const avatar = role === 'user'\n    ? `<div class=\"msg-avatar\">You</div>`\n    : `<div class=\"msg-avatar\">🤖</div>`;\n\n  const docTag = docName\n    ? `<div class=\"msg-doc-tag\">📎 ${docName}</div>`\n    : '';\n\n  const formatted = formatResponse(text);\n\n  div.innerHTML = `${avatar}<div class=\"msg-bubble\">${docTag}${formatted}</div>`;\n  msgs.appendChild(div);\n  msgs.scrollTop = msgs.scrollHeight;\n}\n\nfunction formatResponse(text) {\n  // Convert markdown-like formatting to HTML\n  return text\n    .replace(/\\*\\*(.+?)\\*\\*/g, '<strong>$1</strong>')\n    .replace(/\\*(.+?)\\*/g, '<em>$1</em>')\n    .replace(/^### (.+)$/gm, '<h3>$1</h3>')\n    .replace(/^- (.+)$/gm, '<li>$1</li>')\n    .replace(/(<li>.*<\\/li>)/gs, '<ul>$1</ul>')\n    .replace(/\\n\\n/g, '</p><p>')\n    .replace(/^(?!<[hul])(.+)/gm, (m) => m.startsWith('<') ? m : m)\n    .split('\\n').map(line => {\n      if (line.startsWith('<h3>') || line.startsWith('<ul>') || line.startsWith('<li>')) return line;\n      if (line.trim() === '') return '';\n      return line;\n    }).join('\\n')\n    .replace(/\\n/g, ' ')\n    .replace(/(^|>)([^<]+)(<|$)/g, (m, p, t, n) => {\n      if (p === '>' || n === '<') return m;\n      return p + t + n;\n    });\n}\n\nfunction showTyping() {\n  const msgs = document.getElementById('messages');\n  const id = 'typing-' + Date.now();\n  const div = document.createElement('div');\n  div.className = 'message assistant';\n  div.id = id;\n  div.innerHTML = `<div class=\"msg-avatar\">🤖</div><div class=\"msg-bubble\"><div class=\"typing-indicator\"><div class=\"typing-dot\"></div><div class=\"typing-dot\"></div><div class=\"typing-dot\"></div></div></div>`;\n  msgs.appendChild(div);\n  msgs.scrollTop = msgs.scrollHeight;\n  return id;\n}\n\nfunction removeTyping(id) {\n  const el = document.getElementById(id);\n  if (el) el.remove();\n}\n</script>\n" }} />

      <Script id="inline-ai-support-0" strategy="afterInteractive" dangerouslySetInnerHTML={{ __html: "\n/* ══════════════════════════════════════════════════════════════\n   HR PLAYHOUSE HUB — AI HR SUPPORT\n   Uses Claude API (claude-sonnet-4-20250514) via the Anthropic API\n\n   DEVELOPER NOTE: The Anthropic API does not support direct\n   browser calls (no CORS). This file is designed for:\n   \n   Option A (Recommended): WordPress REST endpoint\n   - Create a WP REST route: POST /wp-json/hrph/v1/chat\n   - That route calls the Anthropic API server-side\n   - Replace API_ENDPOINT below with your WordPress endpoint\n   \n   Option B: Netlify / Vercel serverless function\n   - Deploy a small proxy function\n   - Set ANTHROPIC_API_KEY as an env variable\n   \n   The chat logic, UI, and message handling are complete.\n   Only the API_ENDPOINT URL needs updating.\n══════════════════════════════════════════════════════════════ */\n\nconst API_ENDPOINT = '/wp-json/hrph/v1/chat'; // ← Change this to your proxy endpoint\n\nconst SYSTEM_PROMPT = `You are an expert HR advisor for HR Playhouse Hub, a professional HR learning platform. You provide evidence-based, practical HR guidance to HR professionals at all career stages — from early-career advisors to CHROs.\n\nYour knowledge base includes:\n- CIPD professional frameworks and standards\n- SHRM competency model\n- Gallup engagement research\n- UK employment law (Equality Act 2010, Employment Rights Act 1996, ACAS Codes of Practice)\n- Nigerian Labour Act and National Industrial Court principles\n- US employment law (Title VII, ADA, FMLA, ADEA, at-will doctrine)\n- Singapore Employment Act, MOM guidelines, TAFEP\n- Hong Kong Employment Ordinance\n- Academic HR research and best practice\n\nYour responses are:\n- Practical and actionable — give real guidance, not just principles\n- Jurisdiction-aware — ask which country if it matters for the answer\n- Evidence-based — reference frameworks, legislation, and research when relevant\n- Balanced — acknowledge complexity and nuance in HR situations\n- Professional but accessible — expert tone, not academic jargon\n\nAlways end responses involving employment law with a brief reminder that this is guidance only and a qualified employment lawyer should be consulted for specific legal situations.\n\nIf a document has been shared, analyse it thoroughly and provide specific, detailed feedback relevant to the document type.`;\n\nlet conversationHistory = [];\nlet uploadedFileContent = null;\nlet uploadedFileName = null;\nlet isLoading = false;\n\n/* FILE HANDLING */\nasync function handleFileUpload(input) {\n  const file = input.files[0];\n  if (!file) return;\n  if (file.size > 10 * 1024 * 1024) {\n    alert('File too large. Please upload files under 10MB.');\n    return;\n  }\n  uploadedFileName = file.name;\n  const ext = file.name.split('.').pop().toLowerCase();\n  const iconMap = { pdf: '📕', doc: '📘', docx: '📘', txt: '📄' };\n  document.getElementById('fp-icon').textContent = iconMap[ext] || '📄';\n  document.getElementById('fp-name').textContent = file.name;\n  document.getElementById('upload-default').style.display = 'none';\n  document.getElementById('file-preview').classList.add('show');\n  document.getElementById('upload-zone').classList.add('has-file');\n  // Read as text for TXT files, base64 for others\n  if (ext === 'txt') {\n    uploadedFileContent = await file.text();\n  } else {\n    uploadedFileContent = await toBase64(file);\n  }\n}\n\nfunction toBase64(file) {\n  return new Promise((res, rej) => {\n    const r = new FileReader();\n    r.onload = () => res(r.result.split(',')[1]);\n    r.onerror = rej;\n    r.readAsDataURL(file);\n  });\n}\n\nfunction removeFile(e) {\n  e.stopPropagation();\n  uploadedFileContent = null;\n  uploadedFileName = null;\n  document.getElementById('doc-upload').value = '';\n  document.getElementById('upload-default').style.display = 'block';\n  document.getElementById('file-preview').classList.remove('show');\n  document.getElementById('upload-zone').classList.remove('has-file');\n}\n\n/* CHAT LOGIC */\nfunction autoResize(el) {\n  el.style.height = 'auto';\n  el.style.height = Math.min(el.scrollHeight, 120) + 'px';\n}\n\nfunction handleKeydown(e) {\n  if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); sendMessage(); }\n}\n\nfunction askSuggested(btn) {\n  document.getElementById('chat-input').value = btn.textContent;\n  sendMessage();\n}\n\nfunction newChat() {\n  conversationHistory = [];\n  uploadedFileContent = null;\n  uploadedFileName = null;\n  document.getElementById('doc-upload').value = '';\n  document.getElementById('upload-default').style.display = 'block';\n  document.getElementById('file-preview').classList.remove('show');\n  document.getElementById('upload-zone').classList.remove('has-file');\n  const msgs = document.getElementById('messages');\n  msgs.innerHTML = `<div class=\"welcome-state\" id=\"welcome-state\">\n    <div class=\"ws-icon\">🤖</div>\n    <div class=\"ws-title\">Your AI HR Advisor</div>\n    <div class=\"ws-sub\">Ask any HR question and get research-backed, evidence-led answers grounded in CIPD frameworks, employment law, and best practice.</div>\n    <div class=\"ws-capabilities\">\n      <span class=\"ws-cap\">🇬🇧 UK law</span>\n      <span class=\"ws-cap\">🇳🇬 Nigerian law</span>\n      <span class=\"ws-cap\">🇺🇸 US law</span>\n      <span class=\"ws-cap\">🇸🇬 Singapore law</span>\n      <span class=\"ws-cap\">📚 CIPD-aligned</span>\n      <span class=\"ws-cap\">📎 Document analysis</span>\n    </div>\n  </div>`;\n}\n\nasync function sendMessage() {\n  if (isLoading) return;\n  const input = document.getElementById('chat-input');\n  const text = input.value.trim();\n  if (!text) return;\n\n  // Hide welcome state\n  const ws = document.getElementById('welcome-state-initial');\n  if (ws) ws.remove();\n\n  // Add user message to UI\n  appendMessage('user', text, uploadedFileName);\n  input.value = '';\n  input.style.height = 'auto';\n\n  // Build message for API\n  let userContent;\n  const ext = uploadedFileName ? uploadedFileName.split('.').pop().toLowerCase() : null;\n\n  if (uploadedFileContent && ext === 'txt') {\n    userContent = `${text}\\n\\n[Document attached: ${uploadedFileName}]\\n\\n${uploadedFileContent}`;\n  } else if (uploadedFileContent && (ext === 'pdf' || ext === 'doc' || ext === 'docx')) {\n    userContent = [\n      { type: 'text', text: text },\n      { type: 'document', source: { type: 'base64', media_type: ext === 'pdf' ? 'application/pdf' : 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', data: uploadedFileContent }, title: uploadedFileName }\n    ];\n  } else {\n    userContent = text;\n  }\n\n  // Clear file after sending\n  const sentFileName = uploadedFileName;\n  if (uploadedFileContent) {\n    uploadedFileContent = null;\n    uploadedFileName = null;\n    document.getElementById('doc-upload').value = '';\n    document.getElementById('upload-default').style.display = 'block';\n    document.getElementById('file-preview').classList.remove('show');\n    document.getElementById('upload-zone').classList.remove('has-file');\n  }\n\n  conversationHistory.push({ role: 'user', content: userContent });\n\n  // Show typing indicator\n  const typingId = showTyping();\n  isLoading = true;\n  document.getElementById('send-btn').disabled = true;\n\n  try {\n    const response = await fetch(API_ENDPOINT, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({\n        model: 'claude-sonnet-4-20250514',\n        max_tokens: 1024,\n        system: SYSTEM_PROMPT,\n        messages: conversationHistory\n      })\n    });\n\n    removeTyping(typingId);\n\n    if (!response.ok) throw new Error(`API error: ${response.status}`);\n    const data = await response.json();\n    const assistantText = data.content?.[0]?.text || 'Sorry, I could not generate a response. Please try again.';\n\n    conversationHistory.push({ role: 'assistant', content: assistantText });\n    appendMessage('assistant', assistantText);\n\n  } catch (err) {\n    removeTyping(typingId);\n    appendMessage('assistant', `I'm sorry — there was an error connecting to the AI service. Please try again in a moment.\\n\\nIf the problem persists, please contact contact@thehrplayhousehub.org for support.`);\n    console.error('AI Support error:', err);\n  }\n\n  isLoading = false;\n  document.getElementById('send-btn').disabled = false;\n}\n\nfunction appendMessage(role, text, docName) {\n  const msgs = document.getElementById('messages');\n  const div = document.createElement('div');\n  div.className = `message ${role}`;\n\n  const avatar = role === 'user'\n    ? `<div class=\"msg-avatar\">You</div>`\n    : `<div class=\"msg-avatar\">🤖</div>`;\n\n  const docTag = docName\n    ? `<div class=\"msg-doc-tag\">📎 ${docName}</div>`\n    : '';\n\n  const formatted = formatResponse(text);\n\n  div.innerHTML = `${avatar}<div class=\"msg-bubble\">${docTag}${formatted}</div>`;\n  msgs.appendChild(div);\n  msgs.scrollTop = msgs.scrollHeight;\n}\n\nfunction formatResponse(text) {\n  // Convert markdown-like formatting to HTML\n  return text\n    .replace(/\\*\\*(.+?)\\*\\*/g, '<strong>$1</strong>')\n    .replace(/\\*(.+?)\\*/g, '<em>$1</em>')\n    .replace(/^### (.+)$/gm, '<h3>$1</h3>')\n    .replace(/^- (.+)$/gm, '<li>$1</li>')\n    .replace(/(<li>.*<\\/li>)/gs, '<ul>$1</ul>')\n    .replace(/\\n\\n/g, '</p><p>')\n    .replace(/^(?!<[hul])(.+)/gm, (m) => m.startsWith('<') ? m : m)\n    .split('\\n').map(line => {\n      if (line.startsWith('<h3>') || line.startsWith('<ul>') || line.startsWith('<li>')) return line;\n      if (line.trim() === '') return '';\n      return line;\n    }).join('\\n')\n    .replace(/\\n/g, ' ')\n    .replace(/(^|>)([^<]+)(<|$)/g, (m, p, t, n) => {\n      if (p === '>' || n === '<') return m;\n      return p + t + n;\n    });\n}\n\nfunction showTyping() {\n  const msgs = document.getElementById('messages');\n  const id = 'typing-' + Date.now();\n  const div = document.createElement('div');\n  div.className = 'message assistant';\n  div.id = id;\n  div.innerHTML = `<div class=\"msg-avatar\">🤖</div><div class=\"msg-bubble\"><div class=\"typing-indicator\"><div class=\"typing-dot\"></div><div class=\"typing-dot\"></div><div class=\"typing-dot\"></div></div></div>`;\n  msgs.appendChild(div);\n  msgs.scrollTop = msgs.scrollHeight;\n  return id;\n}\n\nfunction removeTyping(id) {\n  const el = document.getElementById(id);\n  if (el) el.remove();\n}\n" }} />
    </>
  );
}
